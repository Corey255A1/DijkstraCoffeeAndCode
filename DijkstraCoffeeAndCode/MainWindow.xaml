<!-- WUNDERVISION 2018 -->
<Window x:Class="DijkstraCoffeeAndCode.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:DijkstraCoffeeAndCode.Views"
        xmlns:vm="clr-namespace:DijkstraCoffeeAndCode.ViewModels"
        mc:Ignorable="d"
        Title="WunderVision - Dijkstra Coffee And Code" Height="452.406" Width="803.61" PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Graphics.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <vm:CoordinateOffsetConverter x:Key="coordinateOffsetConverter"/>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>

        <ItemsControl Background="AliceBlue" Grid.Row="0" ItemsSource="{Binding Path=DijkstraObjects}"  MouseRightButtonDown="CanvasMouseRightButtonDown" MouseLeftButtonDown="CanvasMouseLeftButtonDown">
            <ItemsControl.Resources>
                <DataTemplate DataType="{x:Type vm:DijkstraNodeViewModel}">
                    <views:NodeElement DataContext="{Binding }" />
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:DijkstraEdgeViewModel}">
                    <Line X1="{Binding X1}" Y1="{Binding Y1}" Y2="{Binding Y2}" X2="{Binding X2}" Stroke="Black" StrokeThickness="20"/>
                </DataTemplate>
            </ItemsControl.Resources>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding Left}"/>
                    <Setter Property="Canvas.Top" Value="{Binding Top}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
        </ItemsControl>

        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button x:Name="solveBtn" Margin="4" Content="Solve" Width="75" Click="solveBtn_Click" Focusable="False"/>
            <Button x:Name="stepBtn" Margin="4" Content="Begin Step" Width="75" Click="stepBtn_Click" Focusable="False"/>

            <Button x:Name="resetBtn" Margin="4" Content="Erase Nodes" Width="75" Click="resetBtn_Click" Focusable="False"/>
            <Button x:Name="resetStep" Margin="4" Content="Reset" Width="75" Focusable="False" Click="resetStep_Click"/>
            
            <TextBox x:Name="infoBox" Width="400" VerticalContentAlignment="Center" TextWrapping="Wrap" Text="Welcome to WunderVision's Dijkstra Demo" IsReadOnly="True"/>
            
        </StackPanel>
        
    </Grid>
</Window>
